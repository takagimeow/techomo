import React from 'react';
import { render, fireEvent, waitFor, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

describe('ワークスペース作成画面', () => {
  describe('名前を入力して保存ボタンを押した場合', () => {
    beforeEach(() => {

    });
    afterEach(() => {
      jest.clearAllMocks();
      jest.resetAllMocks();
    });
    it.todo('Workspaceクラスのインスタンスが作成されて、そのidがlowdbに保存される')
    it.todo('Workspaceクラスのインスタンスが作成されて、そのインスタンスがlowdbに保存される');
  });
});